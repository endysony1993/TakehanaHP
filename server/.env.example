# Server environment variables (copy to server/.env and fill values)

# Basic server
PORT=4000
# Comma-separated list of allowed origins for CORS
# Example: https://your-site.example.com,https://www.your-site.example.com
CORS_ORIGIN=

# Mail (use smtp in production; use log in development)
MAIL_DRIVER=smtp
MAIL_FROM="Takehana Homepage" <no-reply@yourdomain.com>
MAIL_TO=sonynoit@gmail.com

# SMTP settings (production / staging)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Optional diagnostics endpoint token for /api/contact/test and /api/contact/verify
MAIL_TEST_TOKEN=
# Verbose mail logs
MAIL_DEBUG=false
