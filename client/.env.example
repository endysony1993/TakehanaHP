# Frontend example environment variables
# Base URL of the API server (including protocol)
# Example for local dev:
VITE_API_BASE_URL=http://localhost:4000

# If deploying under a subpath (e.g., GitHub Pages /TakehanaHP/), set before build:
# VITE_BASE_PATH=/TakehanaHP/
